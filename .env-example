# Database Configuration
# Option A: Provide full URL
DATABASE_URL=postgresql://username:password@localhost:5432/database_name
# Option B: Or provide parts (preferred for local safety)
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_NAME=your_database
# DATABASE_USER=your_user
# DATABASE_PASSWORD=your_password

# Docker Compose Postgres Credentials (set your own secure values)
# POSTGRES_DB=<your_database_name>
# POSTGRES_USER=<your_database_user>
# POSTGRES_PASSWORD=<your_strong_password>

# Server Configuration
HOST=127.0.0.1
PORT=8000

# Authentication
# Use a strong 32+ char random string in development; set via secret manager in production
JWT_SECRET=change-me-to-a-strong-random-string
